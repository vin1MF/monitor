apiVersion: v1
kind: Service
metadata:
  name: monitor-service
  labels:
    app: monitor
spec:
  type: NodePort
  selector:
    app: monitor
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30081
